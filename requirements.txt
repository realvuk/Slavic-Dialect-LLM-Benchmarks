# Pinned with lower bounds for reproducibility; regenerate exact versions with
# `pip freeze > requirements.lock` if you need a fully frozen environment.
openai>=1.40
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
seaborn>=0.13
matplotlib>=3.7
python-dotenv>=1.0
tabulate>=0.9        # required by pandas.DataFrame.to_markdown
tqdm>=4.66
